package com.clarityenglish.textLayout.elements {
	
	import flashx.textLayout.elements.ListElement;
	
	/**
	 * This class just exists because addIEInfo complains if there is more than one tag exists for an element class (and <ol>, <ul> and <list> all map to ListElement)
	 */
	public class OrderedListElement extends ListElement {

	}
	
}